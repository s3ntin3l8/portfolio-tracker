// Intercepted (`(.)admin`) counterpart of `admin/imports/page.tsx` — identical content,
// reused verbatim; only the surrounding layout (`../layout.tsx`) differs.
export { default } from "../../../admin/imports/page";
