// Intercepted (`(.)admin`) counterpart of `admin/database/page.tsx` — identical content,
// reused verbatim; only the surrounding layout (`../layout.tsx`) differs.
export { default } from "../../../admin/database/page";
