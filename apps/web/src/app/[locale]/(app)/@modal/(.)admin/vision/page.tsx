// Intercepted (`(.)admin`) counterpart of `admin/vision/page.tsx` — identical content,
// reused verbatim; only the surrounding layout (`../layout.tsx`) differs.
export { default } from "../../../admin/vision/page";
