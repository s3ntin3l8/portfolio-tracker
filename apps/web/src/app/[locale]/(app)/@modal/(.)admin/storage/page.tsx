// Intercepted (`(.)admin`) counterpart of `admin/storage/page.tsx` — identical content,
// reused verbatim; only the surrounding layout (`../layout.tsx`) differs.
export { default } from "../../../admin/storage/page";
