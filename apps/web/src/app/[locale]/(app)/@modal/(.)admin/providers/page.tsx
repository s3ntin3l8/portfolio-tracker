// Intercepted (`(.)admin`) counterpart of `admin/providers/page.tsx` — identical content,
// reused verbatim; only the surrounding layout (`../layout.tsx`) differs.
export { default } from "../../../admin/providers/page";
