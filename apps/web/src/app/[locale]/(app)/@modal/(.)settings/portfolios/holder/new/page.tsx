// Intercepted (`(.)settings`) counterpart of `settings/portfolios/holder/new/page.tsx` —
// identical content, reused verbatim; only the surrounding layout (`../../../layout.tsx`)
// differs.
export { default } from "../../../../../settings/portfolios/holder/new/page";
