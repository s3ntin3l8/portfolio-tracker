// Intercepted (`(.)settings`) counterpart of
// `settings/portfolios/holder/[holderId]/page.tsx` — identical content, reused verbatim;
// only the surrounding layout (`../../../layout.tsx`) differs.
export { default } from "../../../../../settings/portfolios/holder/[holderId]/page";
