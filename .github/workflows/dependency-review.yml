name: "Dependency Review"

on:
  pull_request:
    branches: ["main"]

jobs:
  dependency-review:
    # The dependency-review API is free on public repos but requires GitHub Advanced
    # Security on private ones. Skip while private; auto-runs once the repo is public.
    if: ${{ github.event.repository.private == false }}
    permissions:
      contents: read
    uses: s3ntin3l8/.github/.github/workflows/dependency-review.yml@main