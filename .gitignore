node_modules/
dist/
coverage/
.vitest-cache/
data/
*.db

# Next.js / Turborepo build output
.next/
.turbo/
next-env.d.ts
*.tsbuildinfo

# serwist service-worker build output (generated by `next build`)
apps/web/public/sw.js
apps/web/public/sw.js.map
apps/web/public/swe-worker-*.js

# env files (keep .env.example)
.env
.env.*
!.env.example

# Claude Code local plans + scratch (keep settings.json + hooks tracked)
.claude/plans/
.claude/tmp/

# OpenCode local plans
.opencode/plans/

# Rolling log files (enabled via LOG_DIR in .env; see services/api/src/plugins/env.ts)
logs/

# Python (vendored pytr entrypoints)
__pycache__/
*.pyc
.venv-pytr/
.claude/projects/
.superpowers

# Git worktrees
.worktrees/

# Claude Design sync (see .design-sync/NOTES.md) — staged scripts, build output,
# and machine-generated cache; the durable set (config.json, NOTES.md,
# conventions.md, previews/, overrides/) stays tracked.
.ds-sync/
ds-bundle/
apps/web/.ds-sync-css/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
