// @portfolio/db — Drizzle schema + migrations (Postgres). Owned by the API service,
// types shared. Filled in during phase 1. Placeholder export keeps it buildable.
export const DB_PACKAGE = "@portfolio/db";

