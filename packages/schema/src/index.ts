// @portfolio/schema — zod schemas + shared types (transactions, instruments, imports).
// Filled in during phase 1 (data model). Placeholder export keeps the package buildable.
export const SCHEMA_PACKAGE = "@portfolio/schema";

